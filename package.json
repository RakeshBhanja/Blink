{
  "name": "blink-monorepo",
  "version": "1.0.0",
  "description": "Blink Chatting Application Monorepo (FastAPI & React)",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "backend": "uvicorn backend.main:app --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
